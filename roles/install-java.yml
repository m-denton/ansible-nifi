---
- name: Download Java
  command: "wget --no-cookies --no-check-certificate --header 'Cookie: gpw_e24=http%3A%2F%2Fwww.oracle.com%2F; oraclelicense=accept-securebackup-cookie' 'http://download.oracle.com/otn/java/jdk/8u271-b09/61ae65e088624f5aaa0b1d2d801acb16/jdk-8u271-linux-x64.tar.gz'"
  #command: "wget -q -O {{java.java_archive}} --no-check-certificate --no-cookies --header 'Cookie: oraclelicense=accept-securebackup-cookie' {{java.download_url}} creates={{java.java_archive}}"

# - name: Unpack archive
#   command: "tar -zxf {{java.java_archive}} -C {{java.download_folder}} creates={{java.java_name}}"

# - name: Fix ownership
#   file: state=directory path={{java.java_name}} owner=root group=root recurse=yes

# - name: Make Java available for system
#   command: 'alternatives --install "/usr/bin/java" "java" "{{java.java_name}}/bin/java" 2000'

# - name: Clean up
#   file: state=absent path={{java.java_archive}}